# Outputs

Generated output files are not tracked in Git.

Running the application creates:

```text
outputs/preprocessed_telco_churn.csv
```

If the bundled demo dataset is used, the default output file is:

```text
outputs/preprocessed_telco_churn_demo.csv
```
